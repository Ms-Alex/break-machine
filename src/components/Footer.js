import React from 'react';

const Footer = () => {
    return (
        <div className="footer">
            Love to code, but remember to take a break! <span role="img" aria-label="heart">❤️</span>
        </div>
    );
}

export default Footer;
